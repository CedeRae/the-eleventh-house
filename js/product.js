/*const sizeSelection = document.querySelector(".sizes").querySelectorAll("#size");
console.log(sizeSelection);

sizeSelection.forEach(element => {
    element.addEventListener("click", function() {
        sizeSelection.forEach(el => el.classList.remove("active"))

        this.classList.add("active");
    })
});*/